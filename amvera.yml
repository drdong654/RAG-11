meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
  skip: false
run:
  persistenceMount: /data
  image: null
  command: uv
  args: run --no-sync uvicorn api.main:app --host 0.0.0.0 --port 8000
  containerPort: 8000