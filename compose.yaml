services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile

    image: vildly-bot:latest
    container_name: vildly-bot

    restart: unless-stopped

    env_file:
      - .env

    volumes:
      - bot_data:/data

volumes:
  bot_data: